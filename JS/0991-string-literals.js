//Instead of creating the object again from the arg passed,we can use this
const createPerson = (name, age, gender) => {
  {
    name, age, gender;
  }
};
//In the above statement,an object with the same key as arg name & value as arg value is created
