//Export functions & variables in one file , so that they can be reused in another
//export const capitaliseString = (str) => str.toUpperCase();
//or
const capitaliseString = (str) => str.toUpperCase();
export { capitaliseString };
export const foo = "bar";
export const bar = "foo";
